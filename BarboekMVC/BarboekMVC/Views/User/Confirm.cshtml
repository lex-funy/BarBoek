@model BarboekMVC.Models.UserModel

@{
    ViewData["Title"] = "Confirm";
}

<h2>Confirm</h2>

@using (Html.BeginForm(actionName: "Confirm", controllerName: "User", FormMethod.Post))
{
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Confirm">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="UEmail" class="control-label"></label>
                    <input asp-for="UEmail" class="form-control" />
                    <span asp-validation-for="UEmail" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Password" class="control-label"></label>
                    <input asp-for="Password" class="form-control" />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="HerhaalPassword" class="control-label"></label>
                    <input asp-for="HerhaalPassword" class="form-control" />
                    <span asp-validation-for="HerhaalPassword" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-default" />
                </div>
            </form>
        </div>
    </div>
}
    <div>
        <a asp-action="Index">Back to List</a>
    </div>

    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }
